import {Theme} from "../values/theme";

const APPEARANCE_THEME_MAP: Record<string, Theme> = {
    light: Theme.light,
    dark: Theme.dark,
    null: Theme.dark,
};

export default APPEARANCE_THEME_MAP;
